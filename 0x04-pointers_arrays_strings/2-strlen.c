#include "holberton.h"
#include <string.h>

/**
* _strlen - A function that returns the length of a string.
* @s: string.
*
* Return: int.
*/
int _strlen(char *s)
{
	int len = strlen(s);

	return (len);
}
