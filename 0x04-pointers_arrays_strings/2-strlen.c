#include "holberton.h"
#include <string.h>

/**
* _strlen - A function that returns the length of a string.
* @s: the entry of string.
*
* Return: Void.
*/
int _strlen(char *s)
{
	int len = strlen(s);

	return (len);
}
