#include "holberton.h"
#include <string.h>

/**
* _strlen - A function that returns the length of a string.
* @s: string.
*
* Return: counter
*/
int _strlen(char *s)
{
	int counter;

	for (counter = 0; s[counter] != '\0'; counter++)
	{
	}
	return (counter);
}
